# Source code

This public development snapshot currently includes the benchmark-preparation and harvest-integrity utilities together with the frozen configurations and aggregate results.

The remaining training/evaluation scripts will be added after the ongoing post-freeze reviewer controls are consolidated, so the repository exposes one clean reproducible pipeline rather than the internal sequence of experimental stages.

The reserved 2023–2024 holdout is not used by the code committed here.
